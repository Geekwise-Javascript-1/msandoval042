// var userFav = prompt('What is your favority color blue');
// alert(userFav);
// var constant = 11;
// var userResp = prompt('choose a number');
// userResp = parseInt(userResp);
// if (isNaN(userResp)){
//   console.log('Not a number');
//   alert('was that a number');
// }
// else{
//   alert('your number is ' + userResp);
// }
var userAge = prompt('what is your age')
var gender = prompt('what is your gender');
if (userAge >= 21 && gender == 'male'){
  alert('not old enough');}
  else if(userAge >= 21 && gender == 'female'){
    alert('girl, not old enough');
  }
  else{
    alert('underage, soory!!!');
  }

/*ternary*/
(ofAge < 21) ? alert('under 21') : alert('over 21');


// userResp = parseInt(userResp);

// NaN = Not a Number
// console.log( isNaN(5) );  Return false because 5 is a #

// console.log( !isNaN(NaN) );
/*
+ = add
- = subtract
* = multiply
% = divide

++ = incrementing by one
-- = decreasing by one
=== = equals
!= = not equals
!== = not equals
|| = or
&& = and

*/

// parse = turning an integer into a string
